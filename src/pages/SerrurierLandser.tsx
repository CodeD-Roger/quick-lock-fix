import { landserData } from "@/data/landser";
import VillePageLayout from "@/components/ville/VillePageLayout";

const SerrurierLandser = () => <VillePageLayout data={landserData} />;

export default SerrurierLandser;
